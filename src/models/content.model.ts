interface styling {
    styling?: {
        colorScheme: string
    }
}

interface configuration {
    configuration?: {
        sort?: string,
        expandTrigger?: string,
        defaultValue?: string[],
        separator?: string,
        autofocus?: boolean,
        placeholder?: string
    }
}
